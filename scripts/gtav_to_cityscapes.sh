# train
# OUTPUT_DIR, resume, and other options, need to be set in the config file
python train.py -cfg CONFIG_PATH

# test
# OUTPUT_DIR, resume, and other options, need to be set in the config file
python test.py -cfg CONFIG_PATH